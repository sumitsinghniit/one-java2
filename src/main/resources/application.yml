spring:
  datasource:
    url: jdbc:mysql://localhost:3306/capsule
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver
    
  jpa:
    generate-ddl: true
    show-sql: true
    
  main:
    banner-mode: "off"